# 测试题：
# 01.使用模块有哪些优点？
# 02.如何定义一个模块？
# 03.当使用模块时，需要使用Python中的哪个关键字？
# 04.导入模块等同于导入一个__________。
# 05.导入time模块就能够在当前程序中访问该模块中的所有名字，也就是其中所有的变量、函数、对象。那么，可以通过哪两种方法来导入time模块？

# 动手试一试
# 01.编写一个模块，其中定义第13章“动手试一试”中的“用大写字母打印名字”函数。再编写一个程序，导入刚才定义的模块，并调用这个函数。
# 02.修改代码清单15-2，把c_to_f()函数导入到主程序的命名空间中，即修改那段代码，从而可以直接调用c_to_f()函数：
# fahrenheit = c_to_f(celsius)
# 而不是通过模块名来调用。
# fahrenheit = my_module.c_to_f(celsius)
# 03.编写一个小程序，生成介于1和20之间的5个随机整数，并把它们打印出来。
# 04.编写一个小程序，要求它运行30秒，每3秒打印一个随机小数。